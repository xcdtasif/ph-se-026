enum UserRole {
  TENANT
  LANDLORD
  ADMIN
}

enum RentalStatus {
  PENDING
  APPROVED
  REJECTED
  ACTIVE
  COMPLETED
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
}

enum PaymentProvider {
  STRIPE
}

enum PropertyStatus {
  ACTIVE
  INACTIVE
  RENTED
}
