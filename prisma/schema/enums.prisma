enum UserRole {
  TENANT
  LANDLORD
  ADMIN
}

enum RequestStatus {
  MOVE_IN_REQUESTED
  MOVE_IN_APPROVED
  MOVE_IN_REJECTED
  MOVED_IN
  MOVE_OUT_REQUESTED
  MOVE_OUT_APPROVED
  MOVE_OUT_REJECTED
  MOVED_OUT
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
  REFUNDED
}

enum PaymentProvider {
  STRIPE
}

enum PaymentType {
  SECURITY_DEPOSIT
  MONTHLY_RENT
  MOVE_OUT_REFUND
}

enum PropertyStatus {
  AVAILABLE
  RENTED
  UNAVAILABLE
}
