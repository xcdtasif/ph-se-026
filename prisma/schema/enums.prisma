enum UserRole {
  TENANT
  LANDLORD
  ADMIN
}

enum RequestType {
  MOVE_IN
  MOVE_OUT
}

enum RequestStatus {
  MOVE_IN_REQUESTED
  MOVE_IN_APPROVED
  MOVE_IN_REJECTED
  MOVED_IN
  MOVE_OUT_REQUESTED
  MOVE_OUT_APPROVED
  MOVE_OUT_REJECTED
  MOVED_OUT
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
  REFUNDED
}

enum PaymentProvider {
  STRIPE
}

enum PaymentType {
  FIRST_PAYMENT
  MONTHLY_RENT
  REFUND
}

enum PropertyStatus {
  AVAILABLE
  RENTED
  UNAVAILABLE
}
